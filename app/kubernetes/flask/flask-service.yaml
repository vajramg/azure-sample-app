apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  type: ClusterIP
  selector:
    app: my-flask
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      name: tcp-5000
